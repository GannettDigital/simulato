'use strict';

const TYPE_ERROR = require('./type-error.js');
const INVALID_PROPERTY = require('./invalid-property.js');
const INVALID_PATH = require('./invalid-path.js');

module.exports = {
  TYPE_ERROR,
  INVALID_PROPERTY,
  INVALID_PATH,
};
